package com.example.remotepc

import com.freerdp.freerdpcore.application.GlobalApp

/** Initializes the FreeRDP Android runtime once for the whole app process. */
class RemotePcApplication : GlobalApp()
